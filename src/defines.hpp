#ifndef DEFINES_HPP
#define DEFINES_HPP

#define DEBUG         0
#define INVERTED_MAP  1 // In real life, the UST map would be inverted, so north = down, etc. Good for confusing people.
#define LOGGING_TYPED 0 // Delay in logging to emulate typing.
#define LOGGING_DELAY 15 // Milliseconds to delay per char.

// Load the encrypted maps.
#define USE_ENCRYPTED_M 1
#define USE_ENCRYPTED_P 1

#endif